# using react native expo v57 , typescript and react native gesture handler to build this snake game app
# compatable with both android and ios using npm 

# step 1 : npm install (In the current project directory)
# step 2 : npx expo start 